import 'package:flutter/material.dart';
class AppIcons {
static const personIcon = Icon(Icons.person);
static const about = Icon(Icons.quiz);
static const dashBoard = Icon(Icons.dashboard_outlined);
static const arrow = Icon(Icons.arrow_circle_up_rounded);

}